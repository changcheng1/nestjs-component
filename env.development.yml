http:
  host: '127.0.0.1'
  port: 3001

db:
  mysql:
    type: 'mysql'
    host: '127.0.0.1'
    port: 3306
    database: 'nestjsdb'
    username: 'root'
    password: '000000'

# Redis 配置
redis:
  host: '127.0.0.1'  # 本地开发环境
  port: 6379
  db: 0
  password: ''
  maxRetriesPerRequest: 3
  retryDelayOnFailover: 100
  enableReadyCheck: false
  synchronize: true

log:
  level: 'info'
  dir: 'logs'

jwt:
  secret: 'secret'

# 配置说明
# 1. 开发环境：使用本地地址 127.0.0.1
# 2. 端口配置：应用使用 3001 端口，避免与生产环境冲突
# 3. Redis 配置：连接本地 Redis 服务
# 4. 数据库配置：连接本地 MySQL 服务
